<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-md md:rounded-lg md:my-10 max-md:w-full py-8 p-8 max-w-full">

        <h1 class="text-3xl text-green-800 font-bold mb-6 text-center">Order Successful!</h1>

        <p class="text-gray-700 mb-4">Your order has been placed successfully.</p>
        <h2 class="text-xl font-semibold mb-1">Ordered Items:</h2>
        <ul class="list-disc list-inside">
            <% orders.forEach(order=> { %>
                <li class="text-gray-700">
                    <%= order.quantity %>x <%= order.name %>
                </li>
            <% }) %>
        </ul>
    </div>
</body>

</html>